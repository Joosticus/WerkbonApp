@page

@model IndexModel
@{
    ViewData["Title"] = "Dashboard";
}

    <div class="row">
        <div class="col-md-4">
            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Klant.KlantID" class="control-label"></label>
                    <select asp-for="Klant.KlantID" class="form-control" asp-items="ViewBag.KlantID"></select>
                </div>

                <div class="form-group">
                    <label asp-for="Personeel.Username" class="control-label"></label>
                    <select asp-for="Personeel.Username" class="form-control" asp-items="ViewBag.Username"></select>
                </div>

                <div class="form-group">
                    <input type="submit" value="submit" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>